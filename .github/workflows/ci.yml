name: CI Pipeline

on:
  push:
    branches:
      - feature-branch  # 作業用のブランチ名
      - another-feature  # 他の作業用ブランチも指定
  pull_request:
    branches:
      - feature-branch  # 作業用のブランチに対するPR
      - another-feature  # 他の作業用ブランチへのPRも指定

jobs:
  build:
    runs-on: ubuntu-latest  # 使用するOSの指定（Ubuntuを使用）
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v2  # コードをリポジトリから取得

      - name: Set up Node.js
        uses: actions/setup-node@v2  # Node.jsのセットアップ
        with:
          node-version: '16'  # 使用するNode.jsのバージョン

      - name: Install dependencies
        run: yarn install  # 依存関係のインストール

      - name: Run ESLint
        run: yarn lint  # ESLintによるコードチェック

      - name: Run Prettier
        run: yarn prettier --check .  # コードのフォーマットチェック

      - name: Run tests
        run: yarn test  # テスト実行

      - name: Build project
        run: yarn build  # プロジェクトのビルド
